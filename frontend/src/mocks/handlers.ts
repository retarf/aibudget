import type { RequestHandler } from "msw";

// REST API request handlers for tests. The app shell makes no API calls yet,
// so this is empty; feature pages will add handlers here.
export const handlers: RequestHandler[] = [];
